"""
decorators
"""

from setuptools import setup, find_packages
setup(
    name="decorative",
    packages=find_packages(),
    install_requires=[
        "decorator==4.0.9"
    ],
    version="0.1",
)
